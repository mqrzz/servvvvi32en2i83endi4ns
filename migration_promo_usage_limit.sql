-- =====================================================
-- MIGRATION: лимит использований у промокода
-- До этого used_count просто рос без ограничения — общий (не персональный)
-- промокод можно было применять неограниченное число раз, пока его не
-- выключили руками. usage_limit = NULL по умолчанию, т.е. поведение для
-- уже существующих промокодов не меняется, пока лимит не проставлен явно.
-- Применять: psql -U <user> -d <db> -f migration_promo_usage_limit.sql
-- =====================================================

ALTER TABLE promo_codes ADD COLUMN IF NOT EXISTS usage_limit INTEGER;
